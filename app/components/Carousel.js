var React = require('react');

var Carousel = React.createClass({
  render: function () {
    console.log(this.props);
    return (
      <div>Bootstrap Carousel With Show Images Here</div>
    )
  }
})

module.exports = Carousel;
